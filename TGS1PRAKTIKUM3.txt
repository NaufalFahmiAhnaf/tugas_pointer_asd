#include <stdio.h>: Perpustakaan standar
#include <stdlib.h>: Library standar untuk alokasi memori dinamis.
#include <string.h>: L
int getCardValue(char card): Fu
void printCards(char *cards, int length): Fungsi untuk menampilkan urutan kartu.
void swap(char *a, char *b): Berfungsi untuk mengubah po
int sortCards(char *cards, int length): Fungsi untuk mengurutkan kartu dan mengembalikan jumlah pertukaran yang dilakukan.
int main(): Fungsi utama program.
scanf("%d", &n): Meminta input jumlah kartu.
char *cards = (char *)malloc(n * sizeof(char)): Mengalokasikan memori untuk array kartu.
Input nilai kartu disimpan dalam array cards.
int swaps = sortCards(cards, n): Memanggil fungsi unt
Menampilkan jumlah minimal langkah pertukaran.
free(cards): Membebaskan memori yang dialokasikan untuk array kartu.